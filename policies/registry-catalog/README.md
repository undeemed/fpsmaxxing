# Registry catalog

Only documented, versioned Registry settings may be added. Every entry requires an exact hive, path, value name, type, 32/64-bit view, permitted values, supported Windows builds, verification probe, risk class, restart requirement, and rollback behavior.
